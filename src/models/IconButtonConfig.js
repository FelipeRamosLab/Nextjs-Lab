export default class IconButtonConfig {
    constructor({
        id,
        Icon,
        action,
        display
    }) {
        this.id = id;
        this.Icon = Icon;
        this.action = action;
        this.display = display;
    }
}
